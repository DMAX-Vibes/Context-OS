# [Your Name] — Private Overlay (TEMPLATE)

> **This is the example/template.** To use it: `cp YOU_PRIVATE.example.md YOU_PRIVATE.md` and fill in the real file. `YOU_PRIVATE.md` is **git-ignored** (see `.gitignore`), so it never gets committed or pushed. This `.example.md` stays tracked so the structure ships with the template — keep it empty of real data.

Non-career private context. **Opt-in per task** — agents must not load, summarize, or export this unless you explicitly authorize it for the current task (Load Level 4 in `MANIFEST.md`).

## What Goes Here

Sensitive context that's genuinely useful for an agent to know *sometimes* but should never sit in the default, shareable files:

- health / family considerations that affect scheduling or decisions
- sensitive personal logistics
- anything you'd be unhappy to see committed to a repo or pasted into a random tool

## Notes

<!-- Real content goes in YOU_PRIVATE.md, not here. -->

[fill in your real YOU_PRIVATE.md]
